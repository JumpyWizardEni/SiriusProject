<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.siriusproject.coshelek">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SiriusProject">
        <activity android:name=".TransactionAddingActivity" />
        <activity android:name=".ui.view.MainScreenActivity" />
        <activity android:name=".OnBoardingActivity" />
        <activity
            android:name=".ui.view.SplashScreenActivity"
            android:exported="true"
            android:theme="@style/Theme.SiriusProject.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>